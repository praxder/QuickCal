# Suggested Commands

- `rg --files` - inspect repo file list quickly.
- `git status --short` - check workspace state before and after work.
- `openspec list --json` - check active OpenSpec changes. Currently fails until OpenSpec is initialized with changes support.

No build, test, lint, format, or app run commands exist yet because the app scaffold has not been created.