# Task Completion

For future implementation tasks, follow AGENTS.md: format, lint/analyze, and test before handoff once a stack exists. Do not bypass hooks with --no-verify. Do not disable tests to make progress.

Current repo has no implementation toolchain, so completion checks are limited to inspecting git status and any relevant OpenSpec validation once artifacts exist.